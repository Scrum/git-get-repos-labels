{
  "name": "git-get-repos-labels",
  "version": "0.0.0",
  "description": "A tiny helper to get a list of labels from the github repository",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "test": "nyc ava",
    "coverage": "nyc report --reporter=text-lcov | coveralls",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "graphql",
    "got",
    "github",
    "labels"
  ],
  "author": "scrum",
  "license": "MIT",
  "dependencies": {
    "graphql-got": "^0.1.2"
  },
  "devDependencies": {
    "ava": "^2.2.0",
    "coveralls": "^3.0.5",
    "nyc": "^14.1.1",
    "ts-node": "^8.3.0",
    "typescript": "^3.5.3"
  },
  "ava": {
    "babel": false,
    "compileEnhancements": false,
    "extensions": [
      "ts"
    ],
    "require": [
      "ts-node/register"
    ]
  }
}
